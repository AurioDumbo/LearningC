#include <stdio.h>
#include <math.h> //add amth functions
int main(){
    double A = sqrt(9); //square root: to find the square root of a number
    double B = pow(2,4); //Power: the first number is the the base,a abd the second the exp;
    int C = round(3.14); //round away from zero
    int D = ceil(3.14);//round always to up
    int E = floor(3.99); //round always to down
    double F = fabs(-100);//we use to retur the absolute value of x
    double G = log(10);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);
    printf("%.lf",B);
    return 0;

}